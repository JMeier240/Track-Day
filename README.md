# TrackDay Racing — MVP
See docs in `docs/` for architecture, API, hardware plan, and roadmap.

## Quick start
1) `cd server && cp .env.example .env && npm install && npm run dev`
2) Open `frontend/index.html`
3) `cd scripts && npm install && npm run sim`